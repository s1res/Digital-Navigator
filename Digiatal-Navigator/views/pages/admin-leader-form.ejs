<%- include('../partials/header', { title: typeof leader !== 'undefined' && leader ? 'Редактирование руководителя' : 'Новый руководитель', page: 'admin-leader-form' }) %>

  <main>
    <section class="page-header">
      <div class="container">
        <h1><%= typeof leader !== 'undefined' && leader ? 'Редактирование руководителя' : 'Новый руководитель' %></h1>
        <p><%= typeof leader !== 'undefined' && leader ? 'Измените информацию о руководителе' : 'Добавьте нового руководителя отделения' %></p>
      </div>
    </section>

    <section class="admin-section">
      <div class="container">
        <div class="profile-container">
          <div class="profile-card">
            <% if (typeof error !== 'undefined' && error) { %>
              <div class="error-message">
                <%= error %>
              </div>
            <% } %>

            <form action="<%= typeof leader !== 'undefined' && leader ? '/admin/content/leaders/' + leader.id : '/admin/content/leaders' %>" method="POST" class="profile-edit-form">
              <div class="form-group">
                <label for="name">ФИО *</label>
                <input type="text" id="name" name="name" value="<%= typeof leader !== 'undefined' && leader ? leader.name : '' %>" required>
              </div>

              <div class="form-group">
                <label for="role">Должность *</label>
                <input type="text" id="role" name="role" value="<%= typeof leader !== 'undefined' && leader ? leader.role : '' %>" required>
              </div>

              <div class="form-group">
                <label for="bio">Биография</label>
                <textarea id="bio" name="bio" rows="4"><%= typeof leader !== 'undefined' && leader && leader.bio ? leader.bio : '' %></textarea>
              </div>

              <div class="form-group">
                <label for="avatar">Эмодзи-аватар</label>
                <input type="text" id="avatar" name="avatar" value="<%= typeof leader !== 'undefined' && leader && leader.avatar ? leader.avatar : '' %>" placeholder="Например: 👨‍💼">
                <small>Один эмодзи для отображения аватара</small>
              </div>

              <div class="form-actions">
                <button type="submit" class="btn btn-primary">
                  <%= typeof leader !== 'undefined' && leader ? 'Сохранить изменения' : 'Создать руководителя' %>
                </button>
                <a href="/admin/content" class="btn btn-secondary">Отмена</a>
              </div>
            </form>
          </div>
        </div>
      </div>
    </section>
  </main>

<%- include('../partials/footer') %>
